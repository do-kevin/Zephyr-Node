/**
 * Authentication Router
 */

// Dependencies ---------------------------------------- /

const db = require("../models");

// Export Authentication Router ---------------------------------------- /

module.exports = (router) => {

};
